/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "portfolio-backend-6aa0.onrender.com"],
  },
};

module.exports = nextConfig;
